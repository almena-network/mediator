[package]
name = "almena-interop"
description = "Interoperability tests of almena-didcomm against other DIDComm v2 implementations (tests only)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dev-dependencies]
almena-didcomm = { path = "../didcomm" }
almena-mediator = { path = "../mediator" }
didcomm = "0.4.1"
affinidi-messaging-didcomm = "0.15.8"
affinidi-crypto = "0.2.9"
serde_json = "1.0.151"
tokio = { version = "1.53.1", features = ["macros", "rt"] }
